class AppAnalytics {
  const AppAnalytics();

  void track(String name, [Map<String, Object?> params = const {}]) {
    // Analytics sink placeholder. Do not log secrets, passwords, tokens, API keys, or payment payloads.
  }
}
